import { generatePassword } from "./3_mockDep";

describe("3_mockDep_test", () => {
  
  //  const password = jest.fn(() => validatePasswordApi("@83XD310256"));
  // it("should resolve if password is truely", async () => {
  //   await expect(password).toBeTruthy();
  // });
  // it("shuld be smaller than 8 wrods", async () => {
  //   await expect(password.length()).toBeLessThanOrEqual(8);
  // });
  // it("shuld include special character", async () => {
  //   const specialChars = /[`!@#$%^&*()_+\-=\[\]{};':"\\|,.<>\/?~]/;
  //   await expect(result).toMatch(specialChars);
  // });
  // it("should have random number < 10", async () => {
  //   const result = await jest.fn(() => randomizer(1, 10));
  //   expect(result).toBeLessThan(10);
  // });
});
