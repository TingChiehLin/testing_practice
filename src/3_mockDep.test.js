import { generatePassword } from "./3_mockDep";

describe("3_mockDep_test", () => {
  it("should", async () => {

    const result = await generatePassword(5, "@");
    // expect(result).toBe();
  });
});
